language: php
php:
  - "5.3"
  - "5.4"

before_script:
  - ./install_dependencies.sh

script: curl http://localhost/ | grep -F 'hello world!' && curl http://localhost/non_existing_url_is_redirected | grep -F 'hello world!'
